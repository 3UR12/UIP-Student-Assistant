# Tests

This MVP deliberately has no captured Moodle fixtures because they could contain student or course data. Static syntax and manifest checks are documented in [docs/TESTING.md](../docs/TESTING.md); authenticated behavior is verified manually against the student's own browser session.
